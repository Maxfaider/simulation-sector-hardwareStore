#Module env.R
#Variables y funciones de entorno del proyecto.

env <- getwd()

env.data <- paste(env, "data", sep="/")
env.annotations <- paste(env, "data", sep="/")
env.graphics <- paste(env, "data", sep="/")
env.scripts <- paste(env, "data", sep="/")
env.stories <- paste(env, "data", sep="/")
